pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Build completed'
        echo 'second build'
      }
    }

    stage('Test') {
      steps {
        echo 'Testing completed'
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploy completed'
      }
    }

  }
}